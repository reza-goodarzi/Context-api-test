import ProfileHeader from "../components/ProfileHeader";
import Posts from "./Posts";

const Profile = () => {
	return (
		<section>
			<ProfileHeader />
			<Posts />
		</section>
	);
};

export default Profile;
